server:
  port: 8083

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/assessment_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: chetana@123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

judge0:
  api:
    base-url: https://api.judge0.com